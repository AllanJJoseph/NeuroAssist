[build]
builder = "NIXPACKS"

[deploy]
startCommand = "cd backend && python -m uvicorn app.main:app --host 0.0.0.0 --port $PORT"
healthcheckPath = "/"
restartPolicyType = "ON_FAILURE"

[variables]
NIXPACKS_INSTALL_CMD = "cd backend && pip install -r requirements.txt"